let ranks = [1, 2, 3, 4]
// const newArr= ranks.map((e)=>e+1)
// const newArr = ranks.map((e) => { return e + 1 })
console.log(
    ranks.map((e, index) => {
        return (e)
    })
)
console.log(
    ranks.map((e, index) => {
         return (index) 
    })
)


// console.log(newArr)

let newArr = ranks.map((e, index) => {
    if(e%2==0)
    return e
})

console.log(newArr)
























































































